# Remote Middleware

This module is both a component of the Robot Controller project and an installable standalone library. The Robot Middleware library provides utilities for external components to interface with the Robot Controller software. For example, this would include remote control devices that want to communicate with the Robot Controller.

## Standalone Libary Installation

```
cmake -S . -B build
cmake --build build
cmake --install build
```
